# Assesment Planning
import random
# setting up constants to make my program more flexible and easy to change
MIN_QUESTS = 1
MAX_QUESTS = 50
DEFAULT_QUESTS = 5

def main():
    print("WELCOME to the math quiz!!")
    # while true loop and try/except to ask for number of questions. to make sure program doesnt crash on invalid input
    while True:
        user_input = input(f"How many questions ({MIN_QUESTS}-{MAX_QUESTS})? or press Enter for the default, which is {DEFAULT_QUESTS} ")
        if user_input == "":
            num_quests = DEFAULT_QUESTS
            break
        try:
            num_quests = int(user_input)
            if MIN_QUESTS <= num_quests <= MAX_QUESTS:
                break
            else:      # boundary check
                print(f"Please stay between {MIN_QUESTS} and {MAX_QUESTS}.")
        except ValueError:
            print("Invalid input! Please enter a whole number.")
    # make a list to store history and our score counter
    quiz_history=[]
    score= 0


    # Main loop
    #
    #  -Generate 2 random numbers
    # -do the math
    # -ask for input
    # -check if input is correct
    # - if correct, tell them
    # - if wrong, tell them the correct answer
    # -store data using a list, append sub list (question, user answer, correct answer, status)

    # calculate percentage

    # Ask the user if they want to see their history
    #for loop to print each row in quiz_history list
    #Display final summary
main()
